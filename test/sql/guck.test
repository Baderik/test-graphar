# name: test/sql/guck.test
# description: test guck extension
# group: [sql]

# Before we load the extension, this will fail
statement error
SELECT guck('Sam');
----
Catalog Error: Scalar Function with name guck does not exist!

# Require statement will ensure this test is run with this extension loaded
require guck

# Confirm the extension works
query I
SELECT guck('Sam');
----
Guck Sam 🐥

query I
SELECT guck_openssl_version('Michael') ILIKE 'Guck Michael, my linked OpenSSL version is OpenSSL%';
----
true
